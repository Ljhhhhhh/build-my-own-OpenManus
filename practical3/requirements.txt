# 项目3：基础工具框架 - 依赖包
# 基于项目1和项目2的基础，添加工具框架所需的依赖

# 基础依赖（继承自项目1和2）
openai>=1.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# 项目3新增依赖
aiohttp>=3.8.0          # 异步HTTP请求，用于天气API等
jsonschema>=4.0.0       # JSON Schema验证
asyncio                 # 异步编程支持（Python内置）

# 可选依赖
requests>=2.28.0        # 同步HTTP请求（备用）